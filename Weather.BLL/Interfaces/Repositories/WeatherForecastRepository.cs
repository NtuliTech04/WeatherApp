namespace Weather.BLL.Interfaces.Repositories
{
    //public class WeatherForecastRepository : IWeatherForecastRepository
    //{

    //}


    //long currentTimestamp = DateTimeOffset.UtcNow.ToUnixTimeSeconds();
    //var forecastData = mappedForecast.WeatherForecastDataDto.ToList();

    //var currentWeather = forecastData.FirstOrDefault();
    ////var current = new WeatherClientResponseDto();
    //foreach (var weather in forecastData)
    //{
    //    if (Math.Abs(weather.Timestamp - currentTimestamp) <= (3600 * 3))
    //    {
    //        currentWeather = weather;
    //        break;
    //    }
    //}

    //return currentWeather;
}
